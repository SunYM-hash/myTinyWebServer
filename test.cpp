#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main1() {
    /*
    int a = 9, b = 10;
    int c = a & b;
    if ((c = 15) > 0) {
        cout<<c<<endl;
    }
    int *p = nullptr;
    string s1 = "123";
    vector<string> *a1;
    a1 = new vector<string>(10, s1);
    auto it = a1->begin();
    for (auto it = a1->begin(); it != a1->end(); it++ ) {
        cout<<*it<<endl;
    }
    for (int i = 0; i < a1->size(); i++) {
        cout<<*(it + i)<<endl;
    }
    int a0[] = {1,2,4,5,6,7,8,9,0,0,0};
    int *p2 = a0;
    int *p3 = new int[10];
    cout<<p2<<"  "<<a0<<"  "<<&a0[0]<<endl;
    cout<<*p2<<*a0<<a0[0];
    cout<<p3<<"  "<<*p3<<endl;
    delete []p3;
*/
    int a[8] ={};
    cout<<a[2]<<endl;
    
    //if (!p) cout<<"hehe"<<endl;

}
/*
STATE_MACHINE(){
    State cur_State = type_A;
    while(cur_State != type_C){
        Package _pack = getNewPackage();
        switch(){
            case type_A:
                process_pkg_state_A(_pack);
                cur_State = type_B;
                break;
            case type_B:
                process_pkg_state_B(_pack);
                cur_State = type_C;
                break;
        }
    }
}
*/
